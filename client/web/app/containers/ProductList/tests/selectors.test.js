// import { fromJS } from 'immutable';
// import { makeSelectProductListDomain } from '../selectors';

// const selector = makeSelectProductListDomain();

describe('makeSelectProductListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
