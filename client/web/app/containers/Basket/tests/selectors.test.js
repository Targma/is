// import { fromJS } from 'immutable';
// import { makeSelectBasketDomain } from '../selectors';

// const selector = makeSelectBasketDomain();

describe('makeSelectBasketDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
